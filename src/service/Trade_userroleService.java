package service;

import org.apache.log4j.Logger;
import vo.Trade_userrole;

public class Trade_userroleService {
	
		private Logger logger = Logger.getLogger(Trade_userroleService.class);
}
