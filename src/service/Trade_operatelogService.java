package service;

import org.apache.log4j.Logger;
import vo.Trade_operatelog;

public class Trade_operatelogService {
	
		private Logger logger = Logger.getLogger(Trade_operatelogService.class);
}
